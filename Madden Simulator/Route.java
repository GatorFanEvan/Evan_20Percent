class Route{
  
  protected String type;
  protected Receiver x;
  
  public Route(String type, Receiver x){
    this.type = type;
    this.x = x;
    if (type == "Streak" || type == "Go"){
    }
    else {
    }
  }
}